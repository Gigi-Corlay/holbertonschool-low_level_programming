#include <stdio.h>
#include <stdlib.h>

/**
 * main - prints all single digit numbers of base 10 starting from 0
 *
 * Return: 0 (Success)
 */
int main(void)
{
	int nbr = 48;

	while (nbr < 58)
	{
		putchar(nbr);
		nbr++;
	}
	putchar('\n');

	return (0);
}
